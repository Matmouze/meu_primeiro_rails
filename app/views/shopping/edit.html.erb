<h1>Alterar Shopping</h1>
<%= form_for @shopId, url:shopping_update_path(@shopId.id) do |f| %>
	<%= f.label :name, 'Nome' %>
	<%= f.text_field :name %>
	<%= f.label :address, 'Endereço' %>
	<%= f.text_field :address %> 
	<%= f.submit 'Alterar' %>
<% end %>

